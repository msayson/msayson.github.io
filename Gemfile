source 'https://rubygems.org'
gem 'github-pages', group: :jekyll_plugins
gem 'jekyll-paginate'
gem 'jekyll-redirect-from'
gem "webrick"

# When running the Jekyll website locally on a Windows device,
# uncomment the following line and run `bundle install` to install
# Windows Directory Monitor, which is required to automatically
# load recently updated code to the local server.
# gem 'wdm', '>= 0.1.0' if Gem.win_platform?
